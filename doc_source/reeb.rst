Reeb Graph and Merge Trees
*************************************

The module `reeb` contains classes for both the reeb graph and merge tree.

This is the reeb graph class which is the majority of functionality of the package.  This includes funtionality such as assigning function values to nodes in the Reeb graph and plotting the graph.

.. automodule:: cereeberus.reeb.graph
    :members:

This module is the merge tree class, which is a reeb graph with additional properties.

.. automodule:: cereeberus.reeb.merge
    :members: